git config --global user.name "CesarQ05"
git config --global user.email "cquijada05@outlook.es"

git clone https://github.com/CesarQ05/SE.git

git add richard.txt

git commit -m "Este es mi primer archivo"

git push origin main

git checkout -b richard

notepad rama.txt

notepad rama.txt

git add rama.txt

git commit -m "Archivo de nueva rama"

git checkout richard

git push origin Richard

git pull

git checkout main

git merge Richard

git push