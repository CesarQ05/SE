git config --global user.name "richardominq"
git config --global user.email "richard@gmail.com"

git clone https://github.com/richardominq/sep.git

git add richard.txt

git commit -m "Este es mi primer archivo"

git push origin main

git checkout -b richard

notepad rama.txt

notepad rama.txt

git add rama.txt

git commit -m "Archivo de nueva rama"

git checkout richard

git push origin Richard

git pull

git checkout main

git merge Richard

git push